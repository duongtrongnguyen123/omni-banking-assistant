fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
eval_type_backport==0.2.0; python_version < "3.10"
fastembed==0.4.2
scikit-learn==1.5.2
python-multipart==0.0.12
websockets==13.1
python-dotenv==1.0.1
google-generativeai==0.8.3
# Optional persistence backends. Both are tiny pure-Python wheels; the
# session module imports them lazily so the demo still runs if they're
# absent. ``redis-py`` for production deployments, ``fakeredis`` for
# the test suite and for judges who want to see the Redis wire path
# without running a docker container.
redis==5.0.1
fakeredis>=2.20
# Postgres adapter (hien branch — managed-DB read path when
# DATA_BACKEND=postgres). The binary wheel ships with libpq.
psycopg2-binary==2.9.9
# Speech-to-text (main branch — backend/app/speech). CPU-only, lazy-load.
faster-whisper>=1.2.0
# QR generation for the "Nhận tiền" modal + camera-scan transfer
# pre-fill. ``qrcode[pil]`` pulls Pillow (already present) and a tiny
# pure-Python QR encoder — <100KB combined.
qrcode[pil]>=7.4
# Dev-only: ruff is the lint baseline (`ruff check app/` must be
# clean — see pyproject.toml for the rule set). Pinned to a major
# version so CI doesn't flip when upstream rolls out new lint rules.
ruff>=0.15,<1.0
